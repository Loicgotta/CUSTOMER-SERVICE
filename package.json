{
  "name": "customer-service-chatbot-platform",
  "version": "1.0.0",
  "description": "Plateforme de création de chatbots service client avec RAG",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "widget"
  ],
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../widget && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "cd backend && npm run build && cd ../frontend && npm run build && cd ../widget && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
