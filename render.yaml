services:
  - type: web
    name: chatbot-backend
    env: node
    region: oregon
    buildCommand: |
      cd backend && npm install &&
      cd ../frontend && npm install && npm run build &&
      cd ../widget && npm install && npm run build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        value: postgresql://chatbot_user:sdSFzkDxnzPSsnEpafEK0wVYk0Wt5Nra@dpg-d6eh5kggjchc73fdf5f0-a.oregon-postgres.render.com/chatbot_db_4k7q
      - key: OPENAI_API_KEY
        sync: false  # À configurer manuellement dans le dashboard Render
      - key: JWT_SECRET
        sync: false  # À configurer manuellement dans le dashboard Render
      - key: WEBHOOK_URL
        value: https://n8n.srv793731.hstgr.cloud/webhook/mail-rapport
      - key: ALLOWED_ORIGINS
        value: https://customer-service-blqv.onrender.com
